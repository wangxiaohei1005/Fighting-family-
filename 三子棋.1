时间关系，开始的框架已经搭建，需要渐渐加入
#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int menu()
{
	int Input = 0;
	printf("***************************************\n");
	printf("***********  1.开始游戏   *************\n");
	printf("***********  0.结束游戏   *************\n");
	printf("***************************************\n");
	printf("请输入你的选择：\n");
	scanf("%d", &Input);
	return Input;
}
int main()
{
	menu();
	int Input = menu();
	while (1)
	{
		printf("请输入你的选择：\n");
		scanf("%d", &Input);
		if (Input == 1)
		{
			game();
		}
		else if (Input == 0)
		{
			printf("See you!!!");
			break;
		}
		else
		{
			printf("您的输入有误!");
		}
	}

	system("pause");
	return 0;
}
